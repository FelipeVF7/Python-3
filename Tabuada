# Solicita ao usuário para inserir um número
numero = int(input("Digite um número para ver a tabuada: "))

# Imprime a tabuada do número inserido pelo usuário
print(f"\nTabuada do {numero}:\n")

for i in range(1, 11):
    resultado = numero * i
    print(f"{numero} x {i} = {resultado}")
